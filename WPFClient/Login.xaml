<Window x:Class="WPFClient.Login"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WPFClient"
        mc:Ignorable="d"
        Title="Login" Height="178.97" Width="312.875" WindowStartupLocation="CenterScreen">
    <Grid Margin="4">
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <TextBlock Grid.ColumnSpan="2">Username</TextBlock>
        <TextBox x:Name="usernameTextBox" Margin="80,5,10,5" Grid.ColumnSpan="2"></TextBox>
        <TextBlock Grid.Row="1" Grid.ColumnSpan="2">Password</TextBlock>
        <PasswordBox x:Name="passwordTextBox" Grid.Row="1" Margin="80,5,10,5" Grid.ColumnSpan="2"></PasswordBox>
        <StackPanel Grid.Row="2" Margin="10" Orientation="Horizontal" Grid.ColumnSpan="2">
            <Button Width="92" IsDefault="True" Click="Button_Click">Login</Button>
            <Button  Margin="50,0" Width="92" Content="Cancel" IsCancel="True" Click="Button_Click_1"/>
        </StackPanel>
    </Grid>
</Window>

    